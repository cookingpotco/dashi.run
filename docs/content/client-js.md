# Client JS

Client TypeScript attaches only where you register it. The factory throws after
compile so server code cannot import browser modules by accident.

## Custom elements

Use `client.element` for one-off widgets and `client.module` for larger
surfaces.

## Next steps

Back to [Getting started](/docs/getting-started).
