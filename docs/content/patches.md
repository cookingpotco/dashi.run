# Patches

A patch is HTML aimed at an element. A form POST can return several, and the
page updates in place.

## Forms

Handlers can return `patches()` instead of a full document when the client
runtime is present.

## Next steps

See [Client JS](/docs/client-js) for progressive enhancement.
